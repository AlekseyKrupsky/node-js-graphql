type Album {
    id: ID!
    name: String
    released: Int
    artists: [Artist]
    bands: [Band]
    tracks: [Track]
    genres: [Genre]
    image: String
}

type Query {
    albums(limit: Int, offset: Int): [Album]
    album(id: ID!): Album
}

extend type Mutation {
    createAlbum(
        name: String
        released: Int
        artists: [ID!]
        bands: [ID!]
        tracks: [ID!]
        genres: [ID!]
        image: String
    ): Album

    updateAlbum(
        id: ID!
        name: String
        released: Int
        artists: [ID!]
        bands: [ID!]
        tracks: [ID!]
        genres: [ID!]
        image: String
    ): Album

    deleteAlbum(
        id: ID!
    ): DeleteResponse
}
