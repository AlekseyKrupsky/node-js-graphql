type Band {
    id: ID!
    name: String
    origin: String
#    members: [Member]
    website: String
    genres: String
}

type Query {
    bands(limit: Int, offset: Int): [Band]
    band(id: ID!): Band
}